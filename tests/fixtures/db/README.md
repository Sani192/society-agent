# Local database fixtures

If you need database snapshots for demos, store them in this directory using the naming convention:

- `society_agent_demo_YYYYMMDD.sqlite3`

Do not place database files at the repository root.

[pytest]
addopts = -ra
testpaths = tests
pythonpath = .
markers =
    smoke: deterministic critical workflow smoke tests for PR gating
    integration: tests that exercise cross-module/service integration flows
    endpoint: endpoint-level API behavior and contract coverage
